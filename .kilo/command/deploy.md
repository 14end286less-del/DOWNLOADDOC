# Kilo Configuration

commands:
  - name: "deploy"
    description: "Deploy current site to Cloudflare Pages/Workers"
    prompt: "Deploy this project by running wrangler deploy. Confirm the output URL and report any errors."
    tools: ["Bash"]
